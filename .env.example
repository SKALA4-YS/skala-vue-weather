# OpenWeatherMap API Key
# https://openweathermap.org/api 에서 가입 후 My API keys 에서 발급받는다.
# 이 파일을 복사해 .env 로 만들고 키를 채운 뒤 dev 서버를 다시 시작한다.
#   cp .env.example .env
# 발급 직후에는 활성화까지 시간이 걸려 한동안 401이 날 수 있다.
#
# .env 는 커밋되지 않는다. 배포할 때는 호스팅 서비스의 환경 변수 설정에 같은 이름으로 넣는다.
VITE_OPENWEATHER_API_KEY=
